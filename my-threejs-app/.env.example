# Example environment file (Copy to .env, do NOT commit .env)
# Your OpenAI API key (kept on server only; never in frontend)
OPENAI_API_KEY=sk-your-key-here

# Optional: choose a model (kept on server only)
OPENAI_MODEL=gpt-4o-mini

